 {
  "description": "Search the web based on a query",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "The search term or phrase to query the web"
      }
    },
    "required": ["query"]
  }
}