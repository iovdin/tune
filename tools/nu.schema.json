  {
  "description": "Execute a nushell command",
  "parameters": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "The nushell command to execute"
      }
    },
    "required": ["text"]
  }
}
